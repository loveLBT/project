import Koa from 'koa'
/*import compose from 'koa-compose'
import { checkout,handleMessage } from './wechat/middleware'
import config from './constants/config'

const app=new Koa()
const allMiddleWare=compose([checkout(config.wechat),handleMessage])

app.use(allMiddleWare)

app.listen(3000,()=>{
	console.log('the server is starting at port 3000')
})
*/
